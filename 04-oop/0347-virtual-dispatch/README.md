# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. JavaScript resolves overridden methods on the prototype chain at call time, giving dynamic dispatch for free.

## Run

    node main.js
