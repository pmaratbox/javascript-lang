# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. JavaScript expresses traits as mixin functions that each extend a base class.

## Run

    node main.js
