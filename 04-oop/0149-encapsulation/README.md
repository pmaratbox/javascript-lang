# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. JavaScript uses `#`-prefixed private fields enforced by the runtime.

## Run

    node main.js
