# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. Each builder method returns `this`, enabling idiomatic method chaining.

## Run

    node main.js
