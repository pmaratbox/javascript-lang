# 0151 — Singleton

Obtain a singleton instance twice and confirm both references are the same object, printing `same: yes`. A private static field lazily holds the one shared instance.

## Run

    node main.js
