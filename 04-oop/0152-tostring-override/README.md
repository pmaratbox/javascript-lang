# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Defining `toString()` lets template-literal interpolation render the object.

## Run

    node main.js
