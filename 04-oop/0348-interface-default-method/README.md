# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. JavaScript models the default as a base-class method that a subclass overrides via the prototype chain.

## Run

    node main.js
