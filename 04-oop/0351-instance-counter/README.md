# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. JavaScript holds the tally in a `static` class field bumped inside the constructor.

## Run

    node main.js
