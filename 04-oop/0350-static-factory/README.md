# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. JavaScript exposes the factory as a `static` method that parses hex pairs with `parseInt(.., 16)`.

## Run

    node main.js
