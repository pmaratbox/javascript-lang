# 0098 — Memoization

Compute `fibonacci(10)` recursively with memoization (caching each result so it is computed once) and print it: `55`. A `Map` stores each computed result; checking it first turns the exponential recursion into linear work.

## Run

    node main.js
