# 0668 — Factorial

This lesson uses JavaScript's native `BigInt` arbitrary-precision integer type to compute `30!` (the product `1 * 2 * ... * 30`) by repeated multiplication in a loop. The result is exact — no floating-point rounding — and printed with `.toString()`.

## Run

    node main.js
