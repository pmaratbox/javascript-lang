# 0666 — Big integer multiplication

This lesson uses JavaScript's native `BigInt` arbitrary-precision integer type to multiply two large integers, `123456789n * 987654321n`. The result is exact — no floating-point rounding — and printed with `.toString()`.

## Run

    node main.js
