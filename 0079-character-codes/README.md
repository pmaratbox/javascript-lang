# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. `charCodeAt` reads a UTF-16 code unit and `String.fromCharCode` builds a character from one.

## Run

    node main.js
