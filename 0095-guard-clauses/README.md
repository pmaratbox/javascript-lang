# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Early `return`s for the special cases keep the function flat; the last `return` covers everything else.

## Run

    node main.js
