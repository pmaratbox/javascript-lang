# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. A plain factory function maps names to objects via an object literal.

## Run

    node main.js
