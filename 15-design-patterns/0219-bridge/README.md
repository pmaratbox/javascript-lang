# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The shape holds a color implementor and combines its name with the shape name.

## Run

    node main.js
