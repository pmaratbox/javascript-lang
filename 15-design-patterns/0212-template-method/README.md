# 0212 — Template Method

Run an algorithm skeleton whose middle step is overridden, printing `start work end` on one line. The base `run()` fixes the skeleton while a subclass overrides the `step()` hook.

## Run

    node main.js
