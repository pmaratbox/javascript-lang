# 0205 — Decorator

Decorate a base coffee (cost 2) with milk (+1) and sugar (+1), printing the total cost `4`. Each decorator is a closure wrapping the component and adding to its `cost()`.

## Run

    node main.js
