# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. The adapter wraps the source and exposes a `fahrenheit()` method computing the conversion.

## Run

    node main.js
