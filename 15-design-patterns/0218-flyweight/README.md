# 0218 — Flyweight

Share flyweight instances for repeated characters a, b, a and print the number of distinct objects created `2`. A `Map`-backed factory caches one instance per key so repeats reuse it.

## Run

    node main.js
