# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. Leaf and Composite share a `size()` method, the composite reducing over its children.

## Run

    node main.js
