# 0214 — Visitor

Use a visitor to sum the values of a small node tree with leaves 1, 2, 3, printing `6`. Each node's `accept` double-dispatches to the matching visit method on the visitor.

## Run

    node main.js
