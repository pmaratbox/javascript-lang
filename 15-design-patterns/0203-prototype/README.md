# 0203 — Prototype (Clone)

Clone a prototype object holding 1, mutate the clone to 2, and show the original is unchanged, printing `1 2`. Node's `structuredClone` deep-copies the prototype so the original stays intact.

## Run

    node main.js
