# 0206 — Facade

Expose a single facade call that starts three subsystems and reports `ready`. One `start()` method initializes each subsystem in turn and returns the result.

## Run

    node main.js
