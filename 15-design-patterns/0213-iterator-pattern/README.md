# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A class exposes explicit `hasNext()`/`next()` methods driven by a while loop.

## Run

    node main.js
