# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. Promise chaining with `.then` composes the asynchronous steps idiomatically.

## Run

    node main.js
