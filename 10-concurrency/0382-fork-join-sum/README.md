# 0382 — Fork-Join Sum

Recursively fork the sum of [1..8] into halves and join the partial sums, printing `36`. Recursive Promise.all over each half forks the work and joins the partial sums.

## Run

    node main.js
