# 0381 — Parallel Map

Square 1,2,3,4 in parallel and collect the results in input order, printing `1 4 9 16`. Promise.all over a mapped array runs the work concurrently and preserves index order.

## Run

    node main.js
