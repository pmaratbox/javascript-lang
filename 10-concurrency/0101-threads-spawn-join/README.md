# 0101 — Threads: Spawn and Join

Spawn 3 worker threads, wait for all of them to finish, then print `done: 3`. In Node concurrency is modeled with async tasks joined via `Promise.all`.

## Run

    node main.js
