# 0106 — Producer / Consumer

A producer sends 1..5 to a consumer that sums them, printing `15`. An async generator produces the values and the consumer accumulates them with `for await`.

## Run

    node main.js
