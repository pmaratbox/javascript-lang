# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. A flat `Array.fill(Infinity)` DP table makes the unreachable states explicit.

## Run

    node main.js
