# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. A `reduce` total plus a subset-sum to half reduces the problem to one boolean DP.

## Run

    node main.js
