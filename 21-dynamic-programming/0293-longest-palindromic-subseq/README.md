# 0293 — Longest Palindromic Subsequence

Compute the longest palindromic subsequence length of "bbbab", printing `4`. `Array.from` with a factory builds the 2-D table without shared row references.

## Run

    node main.js
