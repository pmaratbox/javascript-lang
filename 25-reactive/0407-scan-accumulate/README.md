# 0407 — Scan (Running Fold)

Implement a scan operator that emits the running accumulation; produce the running sums of 1, 2, 3, 4. In JavaScript the accumulator state is captured in a closure inside the subscribe function, mutated on each `next`.

## Run

    node main.js
