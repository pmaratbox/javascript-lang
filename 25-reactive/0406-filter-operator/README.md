# 0406 — Filter Operator

Implement a filter operator that forwards only values passing a predicate, keeping the even numbers of 1..6. The operator wraps the source's subscribe, gating each pushed value through the predicate closure.

## Run

    node main.js
