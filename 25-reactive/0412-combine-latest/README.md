# 0412 — Combine Latest

Implement combineLatest of two timed streams, emitting the pair of latest values whenever either source emits (once both have emitted). A virtual-time scheduler picks the smallest (time, seq) entry so timed emissions stay fully deterministic in plain Node.

## Run

    node main.js
