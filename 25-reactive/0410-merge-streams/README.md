# 0410 — Merge Streams

Implement merge of two timed streams using a virtual-time scheduler, interleaving them by emission time. In Node, the Observable and scheduler are plain classes and `subscribe` takes an observer object with `next`/`error`/`complete`.

## Run

    node main.js
