# 0417 — BehaviorSubject

Implement a BehaviorSubject that holds a current value and replays it immediately to each new subscriber. In JavaScript a plain class with a `Set` of observer objects keeps the push-based wiring idiomatic and dependency-free.

## Run

    node main.js
