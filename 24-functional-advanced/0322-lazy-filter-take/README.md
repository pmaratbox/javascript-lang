# 0322 — Lazy Filter + Take

From a lazy stream of naturals, filter the even ones and take three, printing `2 4 6`. Generators compose `filter` and `take` so only the needed evens are produced.

## Run

    node main.js
