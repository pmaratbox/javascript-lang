# 0331 — Transducer Pipeline

Compose map(+1) with filter(even) and run it over [1,2,3,4], printing `2 4`. Transducers compose the reducing step transforms so the data is processed in a single pass.

## Run

    node main.js
