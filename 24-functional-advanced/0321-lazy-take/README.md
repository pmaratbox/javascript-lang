# 0321 — Lazy Take

Build a lazy sequence of the natural numbers and take the first five, printing `1 2 3 4 5`. A generator function yields naturals lazily and `take` pulls only what it needs.

## Run

    node main.js
