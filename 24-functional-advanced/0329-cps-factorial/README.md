# 0329 — CPS Factorial

Compute 5! in continuation-passing style, printing `120`. Each recursive call passes an extended continuation and the top-level continuation is the identity.

## Run

    node main.js
