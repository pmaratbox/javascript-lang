# 0332 — Y Combinator

Define factorial via a fixed-point combinator (no named self-recursion) and compute 5!, printing `120`. The applicative-order Y combinator supplies `self` to a non-recursive factorial generator.

## Run

    node main.js
