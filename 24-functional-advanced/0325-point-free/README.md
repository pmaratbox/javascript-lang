# 0325 — Point-Free Style

Express "sum of squares" point-free (compose map-square with sum) and apply it to [1,2,3], printing `14`. `compose` glues `sum` and `mapSquare` with no mention of the argument.

## Run

    node main.js
