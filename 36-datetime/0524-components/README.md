# 0524 — Date Components

Using Luxon's `DateTime`, parse the fixed instant `2026-06-15` in UTC and read its `year`, `month`, and `day` accessors, printing each on its own line.

## Run

    node main.js
