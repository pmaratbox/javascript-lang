# 0522 — Add days

Using the Luxon library, parse the fixed date `2026-06-15` in UTC and add 10 days with `DateTime.plus({ days: 10 })` (duration arithmetic), then format the result as an ISO date with `toISODate()` to print `2026-06-25`.

## Run

    node main.js
