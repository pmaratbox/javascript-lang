# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. A ternary expression evaluates the same code path under each flag value.

## Run

    node main.js
