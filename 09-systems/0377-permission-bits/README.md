# 0377 — Permission Bits

Decode the Unix permission bits 0b101 into the rwx string `r-x`. Bitwise `&` against each positional mask maps set bits to `r`, `w`, `x` and clear bits to `-`.

## Run

    node main.js
