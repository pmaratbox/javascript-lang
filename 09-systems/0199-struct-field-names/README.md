# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. JavaScript introspects an instance's own fields at runtime with `Object.keys`.

## Run

    node main.js
