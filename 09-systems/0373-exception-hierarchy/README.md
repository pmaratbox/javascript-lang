# 0373 — Exception Hierarchy

Throw a specific error subtype and catch it through a base-type handler, printing `caught base`. In JavaScript an `instanceof` check against the base class matches any subclass thrown.

## Run

    node main.js
