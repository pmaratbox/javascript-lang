# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. JavaScript's `try`/`catch`/`finally` runs the `finally` block whether or not an error was thrown.

## Run

    node main.js
