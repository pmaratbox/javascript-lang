# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. JavaScript models enums as a frozen object whose keys preserve declaration order.

## Run

    node main.js
