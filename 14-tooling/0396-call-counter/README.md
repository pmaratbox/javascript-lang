# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A closure with a property on the wrapper holds the count idiomatically in JavaScript.

## Run

    node main.js
