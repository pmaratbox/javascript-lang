# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A tiny helper throws on a false condition, so reaching the end means every check held.

## Run

    node main.js
