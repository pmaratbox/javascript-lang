# 0400 — ASCII Histogram

Render a histogram of [3,1,2] as rows of "#" characters, one bar per line. JavaScript's `String.prototype.repeat` renders each bar in one expression.

## Run

    node main.js
