# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. JavaScript's `String.prototype.padEnd` handles the column alignment directly.

## Run

    node main.js
