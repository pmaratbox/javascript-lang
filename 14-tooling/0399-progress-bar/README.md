# 0399 — Progress Bar

Render a width-10 progress bar at 40% completion, printing `[####------]`. JavaScript's `String.prototype.repeat` builds the filled and empty segments concisely.

## Run

    node main.js
