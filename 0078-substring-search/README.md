# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `String.indexOf` returns the index of the first occurrence, or `-1` if the substring is not found.

## Run

    node main.js
