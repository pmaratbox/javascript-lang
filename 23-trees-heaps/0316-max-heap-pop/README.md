# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. An array-backed heap sifts down on each pop using destructuring swaps.

## Run

    node main.js
