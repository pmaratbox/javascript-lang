# 0535 — Prepend & append

Using Immutable.js, a persistent `List` is grown at both ends: `.unshift(1)` prepends and `.push(4)` appends, each returning a brand-new list while the original stays unchanged. The final list is printed space-joined.

## Run

    node main.js
