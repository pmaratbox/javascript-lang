# 0531 — Immutable set

Build a persistent `Set` of `{1, 2, 3}` with Immutable.js, then call `.add(4)`, which returns a brand-new set rather than mutating in place. The new set has size `4` while the original is untouched at size `3`, demonstrating that updates yield a new collection.

## Run

    node main.js
