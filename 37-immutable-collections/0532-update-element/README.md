# 0532 — Update an element

Using Immutable.js, `List.set(index, value)` returns a brand-new persistent list with the element replaced; the original list is left unchanged, so printing both shows the updated copy followed by the untouched original.

## Run

    node main.js
