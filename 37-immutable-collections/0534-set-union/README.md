# 0534 — Set union

Using Immutable.js, two persistent `Set`s `{1,2,3}` and `{3,4,5}` are combined with `.union()`, which RETURNS A NEW set while leaving the originals unchanged. The elements are sorted and space-joined for deterministic output.

## Run

    node main.js
