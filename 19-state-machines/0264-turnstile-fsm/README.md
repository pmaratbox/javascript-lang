# 0264 — Turnstile FSM

Drive a turnstile (locked/unlocked) with events coin, push, push and print the resulting states `unlocked locked locked`. A nested object maps each state and event to the next state idiomatically.

## Run

    node main.js
