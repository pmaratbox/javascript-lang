# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. An object literal keyed by state gives O(1) lookups in idiomatic JavaScript.

## Run

    node main.js
