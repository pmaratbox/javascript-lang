# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. Advancing a single state index over the text mirrors a minimal pattern-matching automaton.

## Run

    node main.js
