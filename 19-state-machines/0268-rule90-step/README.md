# 0268 — Cellular Automaton Rule 90

Apply one Rule 90 step (new = left XOR right) to the row "00100" with zero boundaries, printing `01010`. The bitwise XOR operator `^` expresses Rule 90 directly over a mapped array.

## Run

    node main.js
