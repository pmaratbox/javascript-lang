# 0267 — Game of Life Step

Advance one step of a vertical blinker on a 3x3 grid (it becomes horizontal); print the grid using . and #. Nested `Array.prototype.map` builds the next generation immutably.

## Run

    node main.js
