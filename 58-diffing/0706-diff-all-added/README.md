# 0706 — All lines added

Diff an empty list `A=[]` against `B=[alpha,beta]` using the jsdiff library's `diffLines`, then count the parts flagged `added`. Because the source is empty, every line is added, so the added count is `2`.

## Run

    node main.js
