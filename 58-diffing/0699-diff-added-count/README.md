# 0699 — Count added lines

Diff `A=[apple,banana,cherry]` against `B=[apple,blueberry,cherry,date]` using the jsdiff library's `diffLines`, then count the parts flagged `added`. The result is the number of added lines: `2`.

## Run

    node main.js
