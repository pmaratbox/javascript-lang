# 0705 — Identical inputs

Diff a line-list `A=[apple,banana,cherry]` against itself (`A -> A`) using the jsdiff library's `diffLines`. Since the inputs are identical, no parts are flagged `added` or `removed`, so the added and removed counts are both zero: `0 0`.

## Run

    node main.js
