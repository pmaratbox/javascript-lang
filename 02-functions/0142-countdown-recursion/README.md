# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Each call collects the current value into an accumulator array that is joined with spaces.

## Run

    node main.js
