# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. Spread syntax appends the head after the recursively reversed tail.

## Run

    node main.js
