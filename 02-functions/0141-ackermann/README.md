# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. The classic two-argument definition recurses on both arguments until `m` reaches zero.

## Run

    node main.js
