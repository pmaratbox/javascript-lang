# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. JavaScript models an ordered enum as a frozen array, with the index playing the role of the ordinal.

## Run

    node main.js
