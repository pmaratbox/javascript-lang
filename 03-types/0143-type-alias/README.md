# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. JavaScript lacks nominal types, so a JSDoc `@typedef` documents the alias.

## Run

    node main.js
