# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. In JavaScript a discriminated union is a plain object with a `tag` field switched on recursively.

## Run

    node main.js
