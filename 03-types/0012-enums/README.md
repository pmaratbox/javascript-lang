# 0012 — Enums

Define a `Color` enum (`RED`, `GREEN`, `BLUE`) and print the integer value of
`GREEN` (1) and `BLUE` (2). JavaScript has no enum type — the common idiom is a
frozen object mapping names to values, where `Object.freeze` prevents
reassignment.

## Run

    node main.js
