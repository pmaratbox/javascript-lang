# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. Serializing both with `JSON.stringify` gives a simple structural comparison.

## Run

    node main.js
