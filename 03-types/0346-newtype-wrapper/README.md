# 0346 — Newtype Wrapper

Wrap raw integers in distinct UserId and ProductId types so they cannot be confused, printing `user-1 prod-2`. JavaScript uses distinct single-field classes with their own `toString` as lightweight newtypes.

## Run

    node main.js
