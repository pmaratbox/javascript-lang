# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. `slice(k)` (tail) concatenated with `slice(0, k)` (head) gives the left rotation.

## Run

    node main.js
