# 0002 — Variables & basic types

Declare `const` bindings for a number, string, and boolean and print each one
with a template literal. JavaScript has only a single `number` type — there
is no separate int/float — so `42` and `3.14` share the same underlying type.

## Run

    node main.js
