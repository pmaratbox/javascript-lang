# 0005 — Loops

Print 1..5 with a classic C-style three-part `for` loop. `let` is
block-scoped to the loop body — `var` would leak the binding into the
enclosing scope. Modern JS also has `for (const x of iterable)` for
iterating collections.

## Run

    node main.js
