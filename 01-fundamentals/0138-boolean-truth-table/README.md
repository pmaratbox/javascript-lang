# 0138 — Boolean Truth Table

For each (a,b) in (T,T),(T,F),(F,T),(F,F) print a, b, and a AND b, a OR b, a XOR b as lowercase booleans, one row per line. Template literals coerce booleans to lowercase `true`/`false`, and `a !== b` models XOR in JavaScript.

## Run

    node main.js
