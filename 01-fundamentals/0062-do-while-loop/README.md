# 0062 — Do-While Loop

Use a do-while loop (whose body runs once before the condition is checked) to print `1`, `2`, and `3` on one line: `1 2 3`. `do { ... } while (cond)` runs the body once and then re-tests, so the first iteration is unconditional.

## Run

    node main.js
