# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. A simple loop with modulo checks and `console.log` is idiomatic in Node.

## Run

    node main.js
