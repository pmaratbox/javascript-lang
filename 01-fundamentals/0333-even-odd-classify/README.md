# 0333 — Classify Even/Odd

Classify each of [1,2,3,4] as odd or even, printing `odd even odd even`. In JavaScript, `Array.prototype.map` with a ternary builds the labels concisely.

## Run

    node main.js
