# 0304 — Connected Components

Count the connected components of the undirected graph with edges (0,1),(1,2),(3,4) over nodes 0..4, printing `2`. A `seen` boolean array plus a recursive DFS counts each unvisited start as a new component.

## Run

    node main.js
