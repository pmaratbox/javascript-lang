# 0298 — Bellman-Ford

On the digraph with a negative edge 0->1(1),1->2(-2),0->2(4), print the shortest distances from node 0 `0 1 -1`. An edge list of `[u, v, w]` triples relaxed V-1 times handles the negative weight cleanly.

## Run

    node main.js
