# 0302 — Kruskal MST

Compute the MST total weight of edges (0,1,1),(1,2,2),(0,2,3) with union-find, printing `3`. Path-compression folded into `find` keeps the disjoint-set helper a couple of lines.

## Run

    node main.js
