# 0084 — Partition

Partition `1, 2, 3, 4, 5, 6` into evens and odds, printing `evens: 2 4 6` and `odds: 1 3 5`. Two `filter` passes split the list; JavaScript has no single partition call.

## Run

    node main.js
