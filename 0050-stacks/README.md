# 0050 — Stacks

Push `1`, `2`, and `3` onto a stack, then pop them all off and print them in last-in-first-out order: `3 2 1`. An array doubles as a stack — `push` appends and `pop` removes the last element, returning LIFO order. There is no dedicated stack type.

## Run

    node main.js
