# 0010 — Structs

Define a `Person` value with a `name` and an `age`, create one ("Ada", 36), and
print each field. JavaScript has no struct type — an object literal
`{ name, age }` is the idiomatic record, read with dot access. For behavior and
a named type, a `class` with a constructor is the alternative.

## Run

    node main.js
