# 0252 — Reverse Bits in a Byte

Reverse the 8 bits of the value 1 (00000001) to get 128 (10000000), printing `128`. A simple loop shifts each bit into the reversed position.

## Run

    node main.js
