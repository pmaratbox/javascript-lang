# 0086 — Sort a Map by Value

Sort the map `{a: 3, b: 1, c: 2}` by value in ascending order and print the entries: `b:1 c:2 a:3`. `Object.entries` gives `[key, value]` pairs; sorting by the numeric value (`x[1] - y[1]`) orders them ascending.

## Run

    node main.js
