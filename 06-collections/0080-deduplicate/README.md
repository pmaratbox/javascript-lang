# 0080 — Deduplicate

Remove duplicates from `1, 2, 2, 3, 1`, keeping the first occurrence of each in order, and print `1 2 3`. A `Set` keeps only the first occurrence of each value and iterates in insertion order; spreading it back into an array yields the deduplicated list.

## Run

    node main.js
