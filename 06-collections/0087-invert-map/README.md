# 0087 — Invert a Map

Invert the map `{a: 1, b: 2, c: 3}` (swap keys and values) and print the result sorted by the new key: `1:a 2:b 3:c`. Each value becomes a key in `inverted`; object keys are strings, so they are mapped to `Number` and sorted numerically.

## Run

    node main.js
