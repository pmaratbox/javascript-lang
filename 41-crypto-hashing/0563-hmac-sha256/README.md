# 0563 — HMAC-SHA256

This lesson uses JavaScript's built-in `node:crypto` library to compute an HMAC-SHA256 digest. We key the HMAC with the secret `key`, feed it the fixed message `hello`, and print the resulting authentication code as lowercase hex.

## Run

    node main.js
