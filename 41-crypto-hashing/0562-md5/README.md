# 0562 — MD5

This lesson uses the built-in `node:crypto` library to compute the MD5 hash of the fixed string `hello`. We create an MD5 hash object, feed it the input bytes, and print the digest as lowercase hexadecimal.

## Run

    node main.js
