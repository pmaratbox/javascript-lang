# 0565 — Base64 encode

Base64-encode the UTF-8 bytes of the string `hello` using Node's built-in `Buffer` API. `Buffer.from('hello')` captures the raw bytes and `toString('base64')` applies the standard Base64 alphabet, producing `aGVsbG8=`.

## Run

    node main.js
