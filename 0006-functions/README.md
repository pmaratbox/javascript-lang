# 0006 — Functions

Define `add(a, b)` returning their sum, call it with `3` and `4`, and print
the result. A `function` declaration is hoisted (usable before its line in
the file). The equivalent arrow form is `const add = (a, b) => a + b;`.

## Run

    node main.js
