# 0569 — YAML sequence

This lesson uses the `js-yaml` library to parse a YAML document containing a sequence (list) under the `fruits` key. After `yaml.load` turns the block list into a JavaScript array, we join its items with commas to print `apple,banana,cherry`.

## Run

    node main.js
