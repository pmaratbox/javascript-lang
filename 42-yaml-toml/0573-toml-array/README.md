# 0573 — TOML array

This lesson uses the `smol-toml` library to parse a TOML array. We parse the document `tags = ["red", "green", "blue"]` and join the resulting `tags` array with commas to print `red,green,blue`.

## Run

    node main.js
