# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. The loop stops while a full window fits (`i + size <= length`); `slice` extracts each overlapping window.

## Run

    node main.js
