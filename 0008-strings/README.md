# 0008 — Strings

Given `name = "world"`, print a greeting, the name in uppercase, and its
length. Template literals (`` `Hello, ${name}!` ``) interpolate;
`.toUpperCase()` returns a new string; `.length` is a property. `.length`
counts UTF-16 code units, which equals the character count for ASCII text.

## Run

    node main.js
