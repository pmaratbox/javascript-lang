# 0606 — Ordered list

Uses the [markdown-it](https://github.com/markdown-it/markdown-it) library (constructed with the `commonmark` preset) to render the Markdown ordered list `1. a\n2. b` to HTML, strips the trailing newline, and prints the `<ol>` with its `<li>` items.

## Run

    node main.js
