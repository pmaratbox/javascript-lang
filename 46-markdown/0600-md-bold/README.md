# 0600 — Bold

Uses the [markdown-it](https://github.com/markdown-it/markdown-it) Markdown library (with the `commonmark` preset) to render the bold (strong) construct `**bold**` to HTML, strips the trailing newline, and prints `<p><strong>bold</strong></p>`.

## Run

    node main.js
