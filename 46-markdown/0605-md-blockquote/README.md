# 0605 — Blockquote

Uses the [markdown-it](https://github.com/markdown-it/markdown-it) library (configured with the `commonmark` preset) to render a Markdown blockquote (`> quote`) to HTML. The rendered output's trailing newline is stripped before printing so the result is byte-identical across renderers.

## Run

    node main.js
