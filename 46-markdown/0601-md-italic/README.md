# 0601 — Italic

Uses the [markdown-it](https://github.com/markdown-it/markdown-it) Markdown library (configured with the `commonmark` preset) to render the inline emphasis `*italic*` to HTML, strips the trailing newline, and prints `<p><em>italic</em></p>`.

## Run

    node main.js
