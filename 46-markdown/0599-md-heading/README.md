# 0599 — Heading

Uses the [markdown-it](https://github.com/markdown-it/markdown-it) library (constructed with the `commonmark` preset) to render the Markdown ATX heading `# Hello` to HTML, strips the trailing newline, and prints `<h1>Hello</h1>`.

## Run

    node main.js
