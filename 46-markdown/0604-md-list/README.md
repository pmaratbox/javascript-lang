# 0604 — Unordered list

Uses the [markdown-it](https://github.com/markdown-it/markdown-it) library (constructed with the `commonmark` preset) to render the Markdown unordered list `- a` / `- b` to HTML, strips the trailing newline, and prints the `<ul>` with its `<li>` items.

## Run

    node main.js
