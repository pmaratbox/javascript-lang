# 0583 — Select by tag

This lesson uses the `cheerio` library to parse a fixed HTML document and query it with CSS selectors. The tag selector `h1` matches the `<h1>` element, and `.text()` extracts its text content, printing `Hello`.

## Run

    node main.js
