# 0590 — Count matches

Uses the `cheerio` HTML-parsing library to load a fixed HTML document and query it with the CSS class selector `.item`; the matched set's `.length` gives the count of elements that match, which is printed as an integer.

## Run

    node main.js
