# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. Spreading to characters, sorting, and re-joining gives a canonical form; equal canonical forms mean an anagram.

## Run

    node main.js
