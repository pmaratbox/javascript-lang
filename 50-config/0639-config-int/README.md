# 0639 — Integer value

This lesson uses the `node-config` library to load `config/default.json` and read the
integer key `retries` with `config.get('retries')`, printing the resolved value `3`.

## Run

    node main.js
