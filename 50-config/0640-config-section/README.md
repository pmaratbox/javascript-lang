# 0640 — Read a section

This lesson uses the `node-config` library to load `config/default.json` and read multiple
keys from the `server` section — `server.host` and `server.port` — formatting them as
`host:port` to print the resolved value `0.0.0.0:8080`.

## Run

    node main.js
