# 0635 — List value

This lesson uses JavaScript's [node-config](https://github.com/node-config/node-config) library to load `config/default.json` and read the array key `hosts` via `config.get('hosts')`, then joins it with commas to print `a,b,c`.

## Run

    node main.js
