# 0633 — Read a value

This lesson uses JavaScript's `node-config` library to load `config/default.json` and read the top-level string key `name`, printing its value `myapp`.

## Run

    node main.js
