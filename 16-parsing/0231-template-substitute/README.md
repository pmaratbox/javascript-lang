# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. A regex with a replacer callback looks up each key in the map idiomatically.

## Run

    node main.js
