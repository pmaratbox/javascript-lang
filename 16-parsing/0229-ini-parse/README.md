# 0229 — Parse INI

Parse the INI text with section [s] and key k=v, printing the flattened entry `s.k=v`. Splitting lines and tracking the current section flattens the structure in JavaScript.

## Run

    node main.js
