# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. We hand-parse by stripping brackets and reducing the split parts in JavaScript.

## Run

    node main.js
