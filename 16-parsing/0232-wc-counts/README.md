# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. Splitting on whitespace and newlines plus the string length gives all three counts in JavaScript.

## Run

    node main.js
