# 0234 — Parse Number Bases

Parse "ff" as hexadecimal (255) and "101" as binary (5), printing `255 5`. The built-in parseInt takes an explicit radix for each base in JavaScript.

## Run

    node main.js
