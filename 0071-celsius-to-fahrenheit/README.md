# 0071 — Celsius to Fahrenheit

Convert `100` and `0` degrees Celsius to Fahrenheit using `F = C * 9 / 5 + 32`, printing `100C = 212F` and `0C = 32F`. JavaScript has only floating-point division, but `c * 9` is divisible by 5 here, so `f` is a whole number.

## Run

    node main.js
