# 0596 — Count book elements

Uses the [fast-xml-parser](https://github.com/NaturalIntelligence/fast-xml-parser) XML library to parse a fixed catalog document, then counts the `<book>` elements (the parsed `catalog.book` array length), printing `2`.

## Run

    node main.js
