# 0597 — Sum numeric children

Uses the [fast-xml-parser](https://github.com/NaturalIntelligence/fast-xml-parser) XML library to parse a fixed catalog document, then parses each book's `<price>` element as an integer and sums them (30 + 45), printing `75`.

## Run

    node main.js
