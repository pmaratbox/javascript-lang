# 0594 — Find all elements

Uses the [fast-xml-parser](https://github.com/NaturalIntelligence/fast-xml-parser) XML library to parse a fixed catalog document, then finds all `<book>` elements and collects each book's `<title>` text, joining them with commas to print `Go,Rust`.

## Run

    node main.js
