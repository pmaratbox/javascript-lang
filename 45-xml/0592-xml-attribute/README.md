# 0592 — Read an attribute

Uses the [fast-xml-parser](https://github.com/NaturalIntelligence/fast-xml-parser) XML library to parse a fixed catalog document, then reads the `id` attribute of the first `<book>` element, printing `b1`.

## Run

    node main.js
