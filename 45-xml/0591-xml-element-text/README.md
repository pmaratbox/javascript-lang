# 0591 — Read element text

Uses the [fast-xml-parser](https://github.com/NaturalIntelligence/fast-xml-parser) XML library to parse a fixed catalog document, then reads the text of the first book's `<title>` element, printing `Go`.

## Run

    node main.js
