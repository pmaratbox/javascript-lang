# 0593 — Nested element

Uses the [fast-xml-parser](https://github.com/NaturalIntelligence/fast-xml-parser) XML library to parse a fixed catalog document, then reads the text of the first book's nested `<price>` element, printing `30`.

## Run

    node main.js
