# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. An array is the stack: `push` on `(`, `pop` on `)`; leftover items (or an early pop on an empty stack) signal an imbalance.

## Run

    node main.js
