# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. A `Map` of value to index lets each complement (`target - nums[i]`) be checked in O(1) as the array is scanned once.

## Run

    node main.js
