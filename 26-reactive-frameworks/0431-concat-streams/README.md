# 0431 — Concat Streams

Use the library's concat operator so the second stream starts only after the first completes. Built with RxJS using `concat` over two synchronous `of` streams.

## Run

    node main.js
