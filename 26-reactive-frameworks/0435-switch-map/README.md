# 0435 — SwitchMap

Use the library's switchMap on a virtual/test scheduler so a new outer value cancels the previous inner stream. RxJS's switchMap with timer sources on a TestScheduler drives the deterministic timing.

## Run

    node main.js
