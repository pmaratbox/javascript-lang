# 0433 — Zip Streams

Use the library's zip operator to pair two streams by index and combine each pair as a+b. Built with RxJS using `zip` to pair two synchronous `of` streams, then `map` to add each pair.

## Run

    node main.js
