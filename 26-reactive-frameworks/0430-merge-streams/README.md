# 0430 — Merge Streams

Use the library's merge operator on a virtual/test scheduler to interleave two timed streams by emission time. Built with RxJS using `merge` and `timer` driven by the `TestScheduler` virtual clock.

## Run

    node main.js
