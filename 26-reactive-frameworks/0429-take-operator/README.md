# 0429 — Take Operator

Use the library's take operator to take the first 3 values of an unbounded stream, then complete. Uses RxJS `generate` for the unbounded naturals source and the `take(3)` operator to complete after three values.

## Run

    node main.js
