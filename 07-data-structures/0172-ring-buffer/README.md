# 0172 — Ring Buffer

Push 1,2,3,4,5 into a fixed capacity-3 ring buffer (overwriting oldest) and print the final contents `3 4 5`. A modular `start` index plus `size` track the circular window over a fixed-length array.

## Run

    node main.js
