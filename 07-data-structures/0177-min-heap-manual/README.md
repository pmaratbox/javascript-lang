# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An array backs the heap with sift-up on push and sift-down on pop, using destructuring swaps.

## Run

    node main.js
