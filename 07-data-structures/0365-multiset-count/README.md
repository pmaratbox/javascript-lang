# 0365 — Multiset Counts

Add 1,1,2 to a multiset; print the count of 1 (2), remove one 1, then print the count of 1 (1), as `2 1`. A `Map` from element to count with the `??` nullish operator handles missing keys cleanly.

## Run

    node main.js
