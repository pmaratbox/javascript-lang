# 0173 — Trie

Insert "cat" and "car" into a trie, then search "car" (yes) and "can" (no), printing `yes no`. Each node keeps a `Map` of child characters and an `end` flag marking a complete word.

## Run

    node main.js
