# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. Idiomatic JavaScript uses Map-backed trie nodes and a DFS over sorted child keys.

## Run

    node main.js
