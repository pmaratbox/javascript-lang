# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. Idiomatic JavaScript stores the last-occurrence table in a Map for the bad-character shift.

## Run

    node main.js
