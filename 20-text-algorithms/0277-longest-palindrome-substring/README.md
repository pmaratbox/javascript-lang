# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. Idiomatic JavaScript expands around each center with a small arrow-function helper.

## Run

    node main.js
