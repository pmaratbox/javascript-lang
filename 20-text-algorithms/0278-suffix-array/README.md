# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Idiomatic JavaScript sorts index positions by comparing sliced suffixes.

## Run

    node main.js
