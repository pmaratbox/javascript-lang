# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. Idiomatic JavaScript builds the prefix-function as a plain array and scans with a single pass.

## Run

    node main.js
