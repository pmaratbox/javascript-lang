# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. Spreading the word into characters, reversing, and re-joining produces the mirror image to compare against the original.

## Run

    node main.js
