# 0459 — Serialize an object

The built-in `JSON.stringify` (the standard `JSON` global) serializes a plain object to a compact JSON string. Object keys are emitted in insertion order, so declaring fields alphabetically (`age`, `name`) yields the canonical compact, alphabetically-keyed JSON.

## Run

    node main.js
