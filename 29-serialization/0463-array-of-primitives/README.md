# 0463 — Array of primitives

Uses the built-in `JSON` library (`JSON.stringify`) to serialize a list of integers into a compact JSON array. Arrays serialize without any inserted whitespace by default, producing canonical compact output.

## Run

    node main.js
