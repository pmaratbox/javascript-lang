# 0461 — Nested object

Serialize an object that contains another object. Uses JavaScript's built-in `JSON` library (`JSON.stringify`) to serialize a `Person` whose `address` field is a nested `Address` object. Object keys are declared in alphabetical insertion order, so `JSON.stringify` produces compact JSON with alphabetically-ordered keys.

## Run

    node main.js
