# 0462 — Array of objects

Uses the built-in `JSON` library (`JSON.stringify`) to serialize a list of plain
objects to a compact JSON array. Each object's keys are declared in alphabetical
insertion order (`age`, `name`), so the serializer emits them alphabetically with
no extra whitespace.

## Run

    node main.js
