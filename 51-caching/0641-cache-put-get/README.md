# 0641 — Put and get

This lesson uses JavaScript's `lru-cache` library to create a strict LRU cache (`new LRUCache({ max: 3 })`), store a value with `set("a", 1)`, and retrieve it with `get("a")`. The cache returns the stored value, so the program prints `1`.

## Run

    node main.js
