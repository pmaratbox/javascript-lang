# 0645 — Cache size

This lesson uses JavaScript's `lru-cache` library (a strict LRU cache). It creates a cache with capacity 5, stores `a=1` and `b=2`, then reads the `size` property to print the number of live entries (`2`).

## Run

    node main.js
