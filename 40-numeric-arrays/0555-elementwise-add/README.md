# 0555 — Element-wise add

Add the vectors `[1, 2, 3]` and `[10, 20, 30]` element-wise using mathjs's `add` operation, which sums corresponding entries of the two arrays. The integer results are printed space-joined with no decimal points.

## Run

    node main.js
