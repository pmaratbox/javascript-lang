# 0557 — Transpose

Transpose the matrix `[[1, 2, 3], [4, 5, 6]]` using mathjs's `transpose` operation, which swaps rows and columns to produce a 3x2 matrix. Each transposed row is printed as space-joined integers with no decimal point.

## Run

    node main.js
