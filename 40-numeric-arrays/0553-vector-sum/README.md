# 0553 — Vector Sum

This lesson uses the mathjs numeric library. We build the vector `[1, 2, 3, 4]` and reduce it to its total with the library's `sum` operation, then print the result as an integer.

## Run

    node main.js
