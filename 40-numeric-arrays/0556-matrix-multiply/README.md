# 0556 — Matrix multiply

Multiply two 2x2 matrices `[[1,2],[3,4]]` and `[[5,6],[7,8]]` using mathjs's `multiply` operation, which performs real matrix multiplication on nested arrays. Each result row is printed as space-joined integers.

## Run

    node main.js
