# 0558 — Scalar multiply

Multiply the matrix `[[1, 2], [3, 4]]` by the scalar `3` using mathjs's `multiply` operation, which scales every element of the nested array. Each row is printed as space-joined integers with no decimal point.

## Run

    node main.js
