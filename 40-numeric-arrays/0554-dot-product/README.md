# 0554 — Dot product

Compute the dot product of the vectors `[1, 2, 3]` and `[4, 5, 6]` using mathjs's `dot` operation, which multiplies corresponding elements and sums them. The integer result is printed with no decimal point.

## Run

    node main.js
