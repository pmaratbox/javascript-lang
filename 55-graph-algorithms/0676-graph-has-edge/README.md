# 0676 — Edge existence

Build the fixed weighted undirected graph G with the **graphlib** library and use its `hasEdge` query to check whether two edges exist: b-c (present) and a-e (absent). Booleans are printed lowercase and space-joined: `true false`.

## Run

    node main.js
