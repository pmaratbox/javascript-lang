# 0675 — Neighbors

This lesson uses the `graphlib` graph library to build the fixed undirected weighted graph G and query the adjacency of a node. We call `Graph.neighbors("a")` to get the nodes adjacent to `a`, then `.sort()` them for a deterministic order and comma-join the result.

## Run

    node main.js
