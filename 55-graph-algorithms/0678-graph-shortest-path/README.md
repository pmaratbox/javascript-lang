# 0678 — Shortest path route

Build the fixed weighted undirected graph G with the **graphlib** library and run its `alg.dijkstra` algorithm from node `a`. Reconstruct the unique weighted shortest path to `e` by following the `predecessor` links and join the node sequence with `-`: `a-b-c-d-e` (total cost 4).

## Run

    node main.js
