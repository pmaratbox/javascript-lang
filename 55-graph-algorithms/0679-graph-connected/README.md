# 0679 — Connectivity

This lesson uses the `graphlib` library on the fixed weighted undirected graph G (nodes a–e). To decide whether a path exists between `a` and `e`, it calls `alg.components`, which partitions the graph into connected components; `a` and `e` are connected when they fall in the same component. The lowercase boolean result is printed.

## Run

    node main.js
