# 0115 — Dispatch Table

Store functions in a map keyed by name, then apply "add" and "mul" to (3,4), printing `7 12`. A plain object whose values are functions serves as an idiomatic dispatch table.

## Run

    node main.js
