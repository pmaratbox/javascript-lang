# 0111 — Partial Application

Partially apply a two-argument add by fixing the first argument to 10, then call the result with 3 to print `13`. JavaScript's `Function.prototype.bind` fixes leading arguments, leaving a one-arg function.

## Run

    node main.js
