# 0118 — Option Map Chaining

Map a function over a present optional (10 -> 12) and an absent one (-> fallback), printing `12 none`. Tagged `some`/`none` objects model an Option whose `map` passes `none` through untouched.

## Run

    node main.js
