# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. JavaScript's `Math.floor` makes the integer-division steps of the formula explicit.

## Run

    node main.js
