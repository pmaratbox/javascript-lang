# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. The loop walks the array left to right, stopping at the first match; `indexOf` is the built-in equivalent.

## Run

    node main.js
