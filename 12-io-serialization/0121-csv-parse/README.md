# 0121 — Parse CSV

Parse the two CSV rows `alice,30` and `bob,25` into name=value pairs and print `alice=30 bob=25`. Splitting each line on the comma and destructuring gives the name and value directly.

## Run

    node main.js
