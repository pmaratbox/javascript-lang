# 0120 — Append to a File

Write "a" to a file, append "b", then read both lines back and print `a b`. Node's `appendFileSync` opens in append mode so the second write keeps the first.

## Run

    node main.js
