# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. JSON.stringify produces compact output with no spaces by default.

## Run

    node main.js
