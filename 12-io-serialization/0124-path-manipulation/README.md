# 0124 — Path Manipulation

Join "/tmp" and "file.txt", then take the basename and extension, printing `/tmp/file.txt file.txt .txt`. Using `path.posix` keeps "/" separators regardless of the host OS.

## Run

    node main.js
