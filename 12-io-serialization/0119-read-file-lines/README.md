# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. In Node, `readFileSync` returns the whole file so you split on newlines and filter out empties.

## Run

    node main.js
