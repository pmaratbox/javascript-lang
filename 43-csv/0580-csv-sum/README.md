# 0580 — Sum a numeric column

This lesson uses the `papaparse` library to parse fixed CSV text. We parse a `name,age,city` document with header rows into objects, convert each row's `age` field to an integer, and sum them (`30 + 25 + 35`) to print `90`.

## Run

    node main.js
