# 0243 — Newton Square Root

Approximate sqrt(2) with Newton iteration and print it to 4 decimals `1.4142`. `Number.prototype.toFixed` handles the fixed-decimal formatting idiomatically.

## Run

    node main.js
