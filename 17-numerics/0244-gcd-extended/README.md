# 0244 — Extended Euclid

Compute extended gcd(30,12) returning g and Bezout coefficients, printing `6 1 -2` (since 30*1+12*(-2)=6). Array destructuring of the recursive return makes the coefficient swap clean in JavaScript.

## Run

    node main.js
