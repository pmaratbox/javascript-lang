# 0236 — Big Number Addition

Add the large numbers 999999999999 and 1 as digit strings, printing `1000000000000`. In modern Node you can use `charCodeAt` to read digits without per-character allocation.

## Run

    node main.js
