# 0250 — Iterative Fast Power

Compute 2^10 by iterative binary exponentiation, printing `1024`. Bitwise `&` and `>>` test and shift the exponent idiomatically in JavaScript.

## Run

    node main.js
