# 0240 — Complex Multiplication

Multiply the complex numbers (1+2i) and (3+4i), printing the real and imaginary parts `-5 10`. Plain object literals with `re`/`im` fields model complex values cleanly in JavaScript.

## Run

    node main.js
