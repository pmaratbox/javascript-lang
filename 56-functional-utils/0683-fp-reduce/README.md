# 0683 — Reduce

Reduce a list to a value. Lodash's `_.reduce` folds `[1,2,3,4,5]` with addition starting from `0`, accumulating the running total into the single result `15`.

## Run

    node main.js
