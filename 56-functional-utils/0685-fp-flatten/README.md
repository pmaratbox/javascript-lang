# 0685 — Flatten

Uses lodash's `_.flatten` to flatten a list of lists one level deep. Given `[[1,2],[3,4],[5,6]]`, it produces a single flat array `[1,2,3,4,5,6]`, which we comma-join for the output.

## Run

    node main.js
