# 0689 — Take

Uses lodash's `_.take` to grab the first N elements from the front of a list. Given `[1..10]` and `n = 3`, it returns `[1,2,3]`, which we comma-join for the output.

## Run

    node main.js
