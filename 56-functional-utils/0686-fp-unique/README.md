# 0686 — Unique

Uses lodash's `_.uniq` to remove duplicates from a list while preserving the order of first occurrence. Given `[1,2,2,3,3,3]`, it produces `[1,2,3]`, which we comma-join for the output.

## Run

    node main.js
