# 0682 — Filter

Filter a list by a predicate. This lesson uses [lodash](https://lodash.com/)'s `_.filter` transform to keep only the even numbers from `[1,2,3,4,5,6]`, then comma-joins the result.

## Run

    node main.js
