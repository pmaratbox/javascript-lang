# 0684 — Chunk

Uses lodash's `_.chunk` to split a list into fixed-size pieces. Here we chunk `[1,2,3,4,5,6]` into size-2 groups, join each chunk with `,` and the chunks with `|` to print `1,2|3,4|5,6`.

## Run

    node main.js
