# 0687 — Zip

Uses lodash's `_.zip` to pair up elements from two lists positionally. Zipping `[1,2,3]` with `["a","b","c"]` yields `[[1,"a"],[2,"b"],[3,"c"]]`, and we format each pair as `<n><s>` before comma-joining for the output.

## Run

    node main.js
