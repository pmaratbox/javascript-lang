# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. A `static` method belongs to the class, not instances, and is called as `MathUtil.square(...)`.

## Run

    node main.js
