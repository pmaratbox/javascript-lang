# 0356 — String<->Int Round Trip

Parse "-42" to the integer -42 and format it back to "-42", printing `-42 -42`. Hand-rolled atoi accumulates digits with a sign, and itoa extracts digits via modulo.

## Run

    node main.js
