# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. Each word's first character is uppercased and concatenated with the rest (`slice(1)`); JavaScript has no built-in title-case.

## Run

    node main.js
