# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `vowels.includes(ch)` checks each character against the vowel set, incrementing the counter on a match.

## Run

    node main.js
